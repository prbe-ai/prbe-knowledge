"""Lane B -- Cross-source LLM-inferred edges.

Sub-packages:
  prompts/   -- versioned system prompts
  bundle     -- builds token-budgeted content bundles from the DB
  extractor  -- validates LLM output and returns InferredEdge list
  worker     -- side-queue drain loop
"""
